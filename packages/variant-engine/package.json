{
  "name": "@pacsarcade/variant-engine",
  "version": "0.3.0",
  "description": "The variants + breakpoints engine for puck-studio — a framework-free registry of variant dimensions (screen and toggle live; group/interaction reserved), combo algebra (ancestry, specificity sort, sparse-merge resolve, definedAt + five-state provenance attribution), and a CSS emitter that writes base-first mobile-first sheets with no !important. Zero dependencies; ships raw TS.",
  "license": "MIT",
  "repository": "github:PacsArcade/puck-studio",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "tsconfig": "*",
    "typescript": "^5.5.4"
  }
}
