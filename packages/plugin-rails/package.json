{
  "name": "@pacsarcade/plugin-rails",
  "version": "0.3.0",
  "description": "The guardrail lint for puck-studio — 13 rules from the Rails Spec, severities read from the brand's tokens per lane (brand/play). A pure engine: same rules in the editor, at publish, and against AI-copilot output.",
  "license": "MIT",
  "repository": "github:PacsArcade/puck-studio",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "peerDependencies": {
    "@pacsarcade/puck-config": ">=0.4",
    "@pacsarcade/variant-engine": ">=0.1"
  },
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "ts-node": "^10.9.2",
    "tsconfig": "*",
    "typescript": "^5.5.4"
  }
}
